# MCP filesystem sandbox

This folder is the **root** passed to `@modelcontextprotocol/server-filesystem` in `examples/.env` (`MCP_STDIO_ARGS`).

Use it to try prompts such as: list files here, read `notes.txt`, or summarize `sample.json`.
